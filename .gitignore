# Independent sub-repos — each has its own git remote and release cadence.
# This repo only versions cross-cutting files (see CLAUDE.md).
/flexi-day/
/flexi-day-be/
/flexi-day-emails/

# Working notes; deliberately local-only.
/todo/

# Dependencies
/node_modules/

# OS / IDE
.DS_Store
.idea/

# Claude Code local overrides
.claude/settings.local.json
